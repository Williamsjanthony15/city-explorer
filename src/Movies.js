import React from 'react';

class Movies extends React.Component {
  render() {
    return (
      <>
      allListGroups = this.props.movies.map((movie, index) => (
        <h1>Movies</h1>
      </>
    )
  }
}

export default Movies;